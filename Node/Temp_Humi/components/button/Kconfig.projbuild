menu "Button Configuration"

    orsource "$IDF_PATH/examples/common_components/env_caps/$IDF_TARGET/Kconfig.env_caps"

    config GPIO_BUTTON
        int "GPIO button pin"
        default 0
        help
            GPIO button pin, don't set target for different boards.

endmenu
